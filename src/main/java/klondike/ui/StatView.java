package klondike.ui;

import javafx.scene.layout.StackPane;
import javafx.stage.*;

public class StatView extends StackPane {

    /**
     * Stage param is so this view gets access to the Primary stage
     */
    public StatView (Stage stage) {
        setStyle("-fx-background-color: #4097be");
    }
    
}
